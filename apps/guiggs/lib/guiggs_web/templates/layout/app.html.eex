<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Guiggs</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
  <ul class="nav nav-pills col-xs-offset-7">
    <li><a href="/">Home </a></li>
    <li><a href="<%= Routes.event_path(@conn, :list) %>"> Events </a></li>
    <li><a href="<%= Routes.login_path(@conn, :index) %>"> Login</a></li>
  </ul>
  <div class="container">
      <header class="header">
        <h1><span class="title">Guiggs</span></h1>
        <h4><span class="sub-title">Your online directory of events</span></h4>
        <%= if @conn.cookies["user_name"] do %>
          <span>
            Hello, <%= @conn.cookies["user_name"] %>
          </span>
        <% end %>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
    </div> <!-- /container -->
  </body>
</html>
